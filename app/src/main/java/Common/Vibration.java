package Common;import android.content.Context;import android.os.Build;import android.os.VibrationEffect;import android.os.Vibrator;public class Vibration {      private static Vibrator vibrate;      private static long delay = 70;      public static void vibrate(Context context) {            vibrate = (Vibrator) context.getSystemService(Context.VIBRATOR_SERVICE);            // Vibrate for 500 milliseconds            if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {                  vibrate.vibrate(VibrationEffect.createOneShot(delay, VibrationEffect.DEFAULT_AMPLITUDE));            } else {                  //deprecated in API 26                  vibrate.vibrate(delay);            }      }}